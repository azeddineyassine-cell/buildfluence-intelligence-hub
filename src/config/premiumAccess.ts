// Premium Access feature flag.
// When false, clicking "Accès Premium" opens the request-access modal
// instead of navigating to the reserved area.
// Flip to true to re-open public access (e.g. once invitations are sent).
export const PREMIUM_ACCESS_OPEN = false;
